<UserControl x:Class="MangosTEx.UserControls.IconsPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <UserControl.Resources>
    </UserControl.Resources>
    
    <WrapPanel HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               Background="LightGray">
        <WrapPanel.Resources>
            <Style TargetType="Rectangle">
                <Setter Property="Width" Value="100" />
                <Setter Property="Height" Value="100" />
                <Setter Property="Fill" Value="Black" />
                <Setter Property="Margin" Value="5" />
            </Style>
        </WrapPanel.Resources>

        <Rectangle OpacityMask="{StaticResource BrushLeftArrow}" />
        <Rectangle OpacityMask="{StaticResource BrushRightArrow}" />
        <Rectangle OpacityMask="{StaticResource BrushEqual}" />
        <Rectangle OpacityMask="{StaticResource BrushNotEqual}" />
        <Rectangle OpacityMask="{StaticResource BrushCross}" />
    </WrapPanel>
</UserControl>
